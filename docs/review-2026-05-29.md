# ML-developer review — 2026-05-29

Snapshot of findings from a subagent code review at commit `4eb0a8d`, what
was patched immediately, and what remains as follow-up.

## Patched in this commit

| ID | file | what changed |
|---|---|---|
| B1 | `src/sycophancy.py:316-327` | sign flip in `find_sycophancy_neurons` — `score = -G * (a_p - a_b)`. Previously ranked anti-sycophancy neurons (ablation would *increase* capitulation). |
| B2 | `src/h7_layers.py:101-114` | activation now captured at forward over prompt+`gen[:ans_pos]` (last position = the one whose logit produced the answer letter). Previously forward+1 produced the position whose logit predicts the *next* token. |
| B3 | `src/h8_xtask.py:148-176` | same off-by-one fix for MCQ + prose attestation activations. Plus drops the unused `_signed_max_at_position` helper in favor of explicit `h[0, -1, :]`. |
| B4 | `src/healthbench.py:299-321` | `_find_answer_token_pos` now uses cumulative decode (`tokenizer.decode(gen_ids[:i+1])`) instead of per-token decode. Robust to BPE byte fallback on multi-byte chars (—, μ, β common in clinical text). |
| B5 | `src/healthbench.py:332-365` | new `_letter_token_id_sets()` returning `{letter: set(first_token_ids)}` across leading-space / bare / newline / tab contexts. `run_one` sums `p_gold_at_answer` over the set. |
| T1 | `tests/test_letter_tokens.py` | regression tests for B4 + B5. |

## Critical bugs flagged but NOT yet patched

| ID | file | description | status |
|---|---|---|---|
| B6 | `src/discover.py:182-183` | H1 score sign convention vs paper Eq.4 ambiguous; symmetric multiplier sweep + magnitude filter mask the issue but anchor `d = a_pos − a_neg` needs unambiguous sign | need 30-q baseline-vs-anchor sanity ablation to confirm direction; document convention |
| B7 | `src/discover.py:111-126` | left-pad-with-zeros for short prompts biases per-token-position means | skip prompts shorter than WINDOW |
| B8 | `src/patching.py:48-67` | `_shape_match` pads zeros on prompt side; clean residuals end up only on prompt-tail, so per-layer score measures commitment-gate not routing | require token-count-matched (clean, corrupt) pairs and drop `_shape_match` |
| B9 | `src/hallucinate.py:84` | `did_commit` checks first 6 token ids; misses `The diagnosis is` because ` diagnosis` and `diagnosis` differ | extend to regex over decoded chunk |

## Compatibility / robustness gaps

| ID | description | severity |
|---|---|---|
| C1 | `setup_caches` uses `os.environ.setdefault` — runtime template can poison HF_HOME to boot disk | medium |
| C2 | Missing `HF_DATASETS_CACHE` (load_dataset on MedMCQA writes to boot disk) | medium |
| C3 | Missing `TRITON_CACHE_DIR`, `CUDA_CACHE_PATH`, `TORCHINDUCTOR_CACHE_DIR` | low |
| C4 | smart-load ladder includes bf16-single-GPU attempt that strictly OOMs on 40GB for 14B; redundant | low |
| C5 | smart-load downgrade to smaller model silently uses `quantize=True` — surprising | low |
| C6 | `BenchmarkRow` jsonl resume crashes on schema changes (added fields) | medium |
| C7 | `gen.scores` retained 310 MB / question; VRAM ratchet on long H6 runs | medium |
| C8 | `mp.Process` workers print to stdout simultaneously → tqdm bars overlap | low |

## ML methodology

| ID | description |
|---|---|
| M1 | H5 N=20 with multiple-testing over ~330K candidates: only "hypothesis generation," not statistically meaningful |
| M2 | H7 / H8 no FDR correction; top-k contains many false positives at r_threshold=0.15 |
| M3 | H4 commit_p_threshold=0.10 too loose for Qwen3 |
| M4 | H6 no prompt-template ensemble; intervention deltas may not exceed prompt noise |
| M5 | Sycophancy missing "user agrees with model" baseline + refusal/hedge category |
| M6 | Consensus-flip heuristic fragile (option-enumeration confound) — use LLM judge |
| M7 | H7 miscal_mcq has length-confound; bin by reasoning chain length |
| M8 | H1 contrastive set needs length / syntax matching |

## Architecture / workflow (high ROI)

| ID | description |
|---|---|
| A1 | Cache (input_ids, generated_ids, ans_pos) per q_id from H6 baseline; reuse in H7/H8 — halves total wall time |
| A2 | `scripts/push_task.py` CLI for the bridge (replace manual git commit cycle) |
| A3 | `BenchmarkRow.from_dict()` with field defaults for resume robustness |
| A4 | Memoize `_letter_token_ids` / `_letter_token_id_sets` on the tokenizer object |
| A5 | Centralize `find_answer_position` (currently 4 near-duplicates) |
| A6 | "Known-good hardware" table in README |

## Test gaps the reviewer wants

| ID | test |
|---|---|
| T2 | CUDA-busy retry ladder reaches attempt 3 |
| T3 | `MODEL_OVERRIDE='Qwen/Qwen3.5-Foo'` strip path |
| T4 | `_patch_mlp` rejects MoE MLPs with clear error |
| T5 | `parse_letter` precedence (`Answer: C` overrides "B is correct because…") |
| T6 | Resume jsonl with old schema doesn't crash |
| T7 | `_shape_match` patches the correct subject-token position |
| T8 | Sycophancy sign — toy 1-layer model with known direction |
| T9 | Bridge `_execute_task` exception path |
| T10 | `hard_reset_repo` drops all `src.*` from sys.modules |
